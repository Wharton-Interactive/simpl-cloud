user {
    ...
    runs [{
        id
        name
        status
        instance {
            name
            status
            url
            player_name
            player_status
        }
    }]
}

run {
    id
    name
    management_url
    players [eid]
    managers [eid]
    instances [{
        name
        status
        players [eid]
    }]
}

games [{
    id
    runs [id]
}]


create_run(name: String!, game_id: Int, multiplayer: Boolean!) {
    id
}

add_player(user_eid: ID!, run_id: ID!) {
    ok
}
    (create user if necessary)
    (create instance if solo game)

set_run_managers(run_id: ID!, user_eids: List) {
    ok
}


GET /user/<pk>/
GET /run/<pk>/
GET /games/
POST /runs/
POST /player/
